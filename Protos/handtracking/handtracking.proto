syntax = "proto3";
package naki3d.common.protocol;

enum HandGestureType {
    // Shared
    GESTURE_NONE = 0; // No detection this frame
    GESTURE_SWIPE_LEFT = 1;
    GESTURE_SWIPE_RIGHT = 2;
    GESTURE_SWIPE_UP = 3;
    GESTURE_SWIPE_DOWN = 4;

    // Mediapipe specific
    GESTURE_CLOSE_HAND = 5;
    GESTURE_OPEN_HAND = 6;
    GESTURE_PINCH = 7;
}

enum HandSide {
    LEFT = 0;
    RIGHT = 1;
}